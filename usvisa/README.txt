If you can use jupyter notebook, use it. Otherwise, use main.py
Firefox is used in demo, you may need to change it.
This is just a prototype with core functions, no notification push, no datebase, no config file. 
log files and std output is included.
You only need to provide the following infomation: userEmail, userPassword and Expected Date(Date before are all included.) And some timeout settings.
target website: ais.usvisa-info.com

How to use:
pip install selenium
pip install tqdm
python main.py

Or use jupter notebook(recommended!)